# TODO purge VACUUM
# TODO allow user to remove generations that only contain S files
# clean Remove redundant generations
#   -- something like...
#   DELETE FROM Files
#   WHERE gid = :gid
#   AND kind = 'S'
#   AND NOT EXISTS (SELECT gid FROM Files WHERE gid = :gid AND kind != 'S')
#
#   DELETE FROM Generations WHERE gid = :gid
#   VACUUM

store
app-1.tm
actions-1.tm
gui-1.tm
store-1.tm
filedata-1.tm
diff-1.tm
globals-1.tm # VERSION
misc-1.tm

test_store.tcl

sql/prepare.sql
sql/create.sql
sql/insert.sql

README.md

st.sh

# vim: syn=yaml
