# TODO purge VACUUM
# TODO if no changes needed & no comments don't add a new generation &
# print "no updates needed"
# TODO allow user to remove generations that only contain S files
# clean Remove redundant generations
#   -- something like...
#
#   CREATE VIEW EmptyGenerations AS
#	SELECT UNIQUE gid FROM Files WHERE kind = 'S' AND gid NOT IN (
#	    SELECT gid FROM Files WHERE kind in ('U', 'Z');
#
#   DELETE FROM Files, Generations WHERE gid in EmptyGenerations;
#   VACUUM

store
app-1.tm
actions-1.tm
gui-1.tm
store-1.tm
filedata-1.tm
diff-1.tm
globals-1.tm # VERSION
misc-1.tm

test_store.tcl

sql/prepare.sql
sql/create.sql
sql/insert.sql

README.md

st.sh

# vim: syn=yaml
