# TODO add support for -D or --debug as "verbose" options
# do _not_ show in usage message; if debug set VERBOSE to 3
# then add; if {$::VERBOSE > 2} ... where needed
# and add global proc dbg {message} function which outputs a unique color to
# stderr (dark red?)

store
gui-1.tm # GUI TODO

str # CLI
app-1.tm
actions-1.tm
store-1.tm
filedata-1.tm
filesize-1.tm
diff-1.tm
globals-1.tm # VERSION
misc-1.tm

test_store.tcl

sql/prepare.sql
sql/create.sql
sql/insert.sql

# TODO add installation instructions once I can built a stand-alone binary
README.md

st.sh

# vim: syn=yaml
